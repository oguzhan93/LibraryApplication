spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/LibraryApplication
    username: postgres
    password: Os123321**
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

cors:
  allowed-origins: "http://localhost:3000,http://localhost:8080,http://localhost:4200"